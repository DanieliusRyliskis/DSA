# memo = {}
# def dfs(i);
#   if base case:
#       return 0 
#   if i in memo:
#       return memo[i] 

#   memo[i] = calculation logic (min, max, etc.) could add +1 or other value next dfs(i)
#   return memo[i]




