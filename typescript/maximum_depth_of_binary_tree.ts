// function walk(curr: TreeNode | null): number {
//   if (!curr) return 0;
//
//   const l: number = walk(curr.left)
//   const r: number = walk(curr.right)
//   return 1 + Math.max(l, r)
// }

function maxDepth(root: TreeNode | null): number {
  // return walk(root)
};
